<!DOCTYPE html>
<html lang="en">
<head>
  <% if flash[:success] %>
    <div class="flash-success"><%= flash[:success] %></div>
  <% end %>
  <% if flash[:error] %>
    <div class="flash-error"><%= flash[:error] %></div>
  <% end %>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans bg-gradient-to-br from-[#a2d2ff] via-[#bde0fe] to-[#cdb4db] flex flex-col items-center min-h-screen p-6 text-gray-900">

  <div class="w-full max-w-lg bg-white/25 backdrop-blur-xl rounded-2xl p-6 shadow-xl">
    <h1 class="text-2xl font-semibold text-center text-sky-700 mb-4">Edit Product</h1>

    <form action="/products/<%= @product['id'] %>/update" method="post" class="space-y-4">
      <div>
        <label class="block font-medium mb-1">Name</label>
        <input type="text" name="name" value="<%= @product['name'] %>"
               class="w-full p-3 rounded-lg bg-white/50 focus:ring-2 focus:ring-sky-400 outline-none">
      </div>

      <div>
        <label class="block font-medium mb-1">Quantity</label>
        <input type="number" name="quantity" value="<%= @product['quantity'] %>"
               class="w-full p-3 rounded-lg bg-white/50 focus:ring-2 focus:ring-sky-400 outline-none">
      </div>

      <div>
        <label class="block font-medium mb-1">Price</label>
        <input type="number" step="0.01" name="price" value="<%= @product['price'] %>"
               class="w-full p-3 rounded-lg bg-white/50 focus:ring-2 focus:ring-sky-400 outline-none">
      </div>

      <div>
        <label class="block font-medium mb-1">Category (optional)</label>
        <input type="text" name="category"
               value="<%= (@prefill_category && !@prefill_category.empty?) ? @prefill_category : (@product['category'] || '') %>"
               placeholder="Type a category — or invent one"
               class="w-full p-3 rounded-lg bg-white/50 focus:ring-2 focus:ring-sky-400 outline-none">

        <!-- ✅ Suggest button now uses formaction instead of a nested form -->
        <button formaction="/products/<%= @product['id'] %>/suggest_category" formmethod="post"
                class="mt-2 px-3 py-2 rounded-md text-sm font-semibold bg-gray-800 text-white hover:bg-gray-700">
          🤖 Suggest
        </button>
      </div>

      <div class="flex gap-3">
        <button type="submit"
                class="bg-gradient-to-r from-cyan-400 to-indigo-500 text-white px-5 py-3 rounded-lg text-sm font-semibold shadow hover:scale-105 hover:from-indigo-500 hover:to-cyan-400 transition">
          Save Changes
        </button>
        <a href="/products"
           class="px-5 py-3 rounded-lg text-sm font-semibold bg-gray-200 text-gray-800 hover:bg-gray-300 transition">
          Cancel
        </a>
      </div>
    </form>

    <!-- Preview current category as Purple Glow tag -->
    <div class="mt-6">
      <div class="text-sm text-gray-700">Current Category Preview:</div>
      <% cat = (@prefill_category && !@prefill_category.empty?) ? @prefill_category : (@product['category'] || "") %>
      <span class="inline-block mt-2 px-2 py-1 rounded-md text-xs font-semibold
                   <%= cat.to_s.strip.empty? ? 'bg-gray-100 text-gray-600' : 'bg-purple-100 text-purple-700' %>">
        <%= cat.to_s.strip.empty? ? "—" : cat %>
      </span>
    </div>
  </div>

</body>
</html>